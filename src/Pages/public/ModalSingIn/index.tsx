import { View, Text } from 'react-native';
import React from 'react';

import styles from './styles';

const ModalSingIn: React.FC = () => (
    <View style={[styles.contain]}>
        <Text>index</Text>
    </View>
);

export default ModalSingIn;
